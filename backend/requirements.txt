# Runtime dependencies only — this file is what the Docker image installs.
# Test/development tooling lives in requirements-dev.txt so it never ships.
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-multipart>=0.0.9
